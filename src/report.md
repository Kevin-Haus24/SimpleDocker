## Part 1

- **Возьми официальный докер-образ с `nginx` и выкачай его при помощи `docker pull`.**
  ![pull](img/Task1/1.png)
- **Проверь наличие докер-образа через `docker images`.**
  ![images](img/Task1/2.png)
- **Запусти докер-образ через `docker run -d [image_id|repository]`.**
  ![run](img/Task1/3.png)
- **Проверь, что образ запустился через `docker ps`.**
  ![ps](img/Task1/4.png)
- **Посмотри информацию о контейнере через `docker inspect [container_id|container_name]`.**
  ![inspect](img/Task1/5.png)
- **По выводу команды определи и помести в отчёт размер контейнера, список замапленных портов и ip контейнера.**
  ![size](img/Task1/6.png) ![port](img/Task1/7.png) ![ip](img/Task1/8.png)
- **Останови докер контейнер через `docker stop [container_id|container_name]`.**
  ![stop](img/Task1/9.png)
- **Проверь, что контейнер остановился через `docker ps`.**
  ![ps](img/Task1/10.png)
- **Запусти докер с портами `80` и `443` в контейнере, замапленными на такие же порты на локальной машине, через команду `run`.**
  ![run](img/Task1/11.png)
- **Проверь, что в браузере по адресу `localhost:80` доступна стартовая страница `nginx`.**
  ![localhost](img/Task1/12.png)
- **Перезапусти докер контейнер через docker `restart [container_id|container_name]`.**
  ![restart](img/Task1/13.png)
- **Проверь любым способом, что контейнер запустился.**
  ![ps](img/Task1/14.png)

## Task 2

- **Прочитай конфигурационный файл `nginx.conf` внутри докер контейнера через команду `exec`.**
  ![exec](img/Task2/1.png)
- **Создай на локальной машине файл `nginx.conf`.**
  ![nginx.conf](img/Task2/2.png)
- **Настрой в нем по пути `/status` отдачу страницы статуса сервера `nginx`.**
  ![status](img/Task2/3.png)
- **Скопируй созданный файл `nginx.conf` внутрь докер-образа через команду `docker cp`.**
  ![cp](img/Task2/4.png)
- **Перезапусти `nginx` внутри докер-образа через команду `exec`.**
  ![exec](img/Task2/5.png)
- **Проверь, что по адресу `localhost:80/status` отдается страничка со статусом сервера `nginx`.**
  ![status](img/Task2/6.png)
- **Экспортируй контейнер в файл `container.tar` через команду `export`.**
  ![export](img/Task2/7.png)
- **Останови контейнер.**
  ![stop](img/Task2/8.png)
- **Удали остановленный контейнер.**
  ![rm](img/Task2/9.png)
- **Удали образ через `docker rmi [image_id|repository]`, не удаляя перед этим контейнеры.**
  ![rmi](img/Task2/10.png)
- **Импортируй контейнер обратно через команду `import`.**
  ![import](img/Task2/11.png)
- **Запусти импортированный контейнер.**
  ![run](img/Task2/12.png)
- **Проверь, что по адресу `localhost:80/status` отдается страничка со статусом сервера `nginx`.**
  ![curl](img/Task2/13.png)

## Task 3

- **Напиши мини-сервер на `C` и `FastCgi`, который будет возвращать простейшую страничку с надписью `Hello, World!`.**
  ![FastCgi](img/Task3/1.png)

- **Запусти написанный мини-сервер через `spawn-fcgi` на порту `8080`.**
  ![spawn-fcgi](img/Task3/2.png)

- **Напиши свой `nginx.conf`, который будет проксировать все запросы с `81` порта на `127.0.0.1:8080`.**
  ![nginx.conf](img/Task3/3.png)

- **Проверь, что в браузере по `localhost:81` отдается написанная тобой страничка.**
  ![localhost](img/Task3/5.png)

- **Положи файл nginx.conf по пути ./nginx/nginx.conf (это понадобится позже).**
  ![nginx](img/Task3/6.png)

## Task 4

- **Напиши свой докер-образ, который:**
  1) собирает исходники мини сервера на FastCgi из Части 3;
  2) запускает его на 8080 порту;
  3) копирует внутрь образа написанный ./nginx/nginx.conf;
  4) запускает nginx.

  ![docker](img/Task4/1.png)

- **Собери написанный докер-образ через docker build при этом указав имя и тег.**
  ![build](img/Task4/2.png)

- **Проверь через docker images, что все собралось корректно.**
  ![images](img/Task4/3.png)

- **Запусти собранный докер-образ с маппингом 81 порта на 80 на локальной машине и маппингом папки ./nginx внутрь контейнера по адресу, где лежат конфигурационные файлы nginx'а (см. Часть 2).**
  ![run](img/Task4/4.png)

- **Проверь, что по localhost:80 доступна страничка написанного мини сервера.**
  ![localhost](img/Task4/5.png)

- **Допиши в ./nginx/nginx.conf проксирование странички /status, по которой надо отдавать статус сервера nginx.**
  ![nginx](img/Task4/6.png)

- **Пересобери докер-образ.**
  ![build](img/Task4/7.png)

- **Проверь, что теперь по localhost:80/status отдается страничка со статусом nginx**
  ![localhost](img/Task4/8.png)

## Task 5

- **Просканируй образ из предыдущего задания через `dockle [image_id|repository]`.**
  ![dockle](img/Task5/1.png)

- **Исправь образ так, чтобы при проверке через `dockle` не было ошибок и предупреждений.**
  ![dockle](img/Task5/2.png)

## Task 6

- **Напиши файл `docker-compose.yml`, с помощью которого: <br>1) Подними докер-контейнер из Части 5 (он должен работать в локальной сети, т. е. не нужно использовать инструкцию `EXPOSE` и мапить порты на локальную машину). <br>2) Подними докер-контейнер с `nginx`, который будет проксировать все запросы с `8080` порта на `81` порт первого контейнера.**
  ![dockle](img/Task6/1.png)

- **Замапь `8080` порт второго контейнера на `80` порт локальной машины.**
  <br>![dockle](img/Task6/2.png)

- **Останови все запущенные контейнеры.**
  ![dockle](img/Task6/3.png)

- **Собери и запусти проект с помощью команд `docker-compose build` и `docker-compose up`.**
  ![dockle](img/Task6/4.png)

- **Проверь, что в браузере по `localhost:80` отдается написанная тобой страничка, как и ранее.**
  ![dockle](img/Task6/5.png)