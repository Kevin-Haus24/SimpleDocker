services:
  backend:
    build:
      context: ./backend
    container_name: backend

  frontend:
    image: nginx:stable
    container_name: frontend
    ports:
      - "80:8080"
    volumes:
      - ./frontend/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend